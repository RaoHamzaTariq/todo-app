apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app.audit-log-service.fullname" . }}
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app: audit-log-service
spec:
  type: {{ .Values.eventServices.auditLog.service.type }}
  ports:
    - port: {{ .Values.eventServices.auditLog.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-app.selectorLabels" . | nindent 4 }}
    app: audit-log-service