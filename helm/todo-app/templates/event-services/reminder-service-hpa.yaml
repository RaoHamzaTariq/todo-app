{{- if .Values.eventServices.reminder.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "todo-app.reminder-service.fullname" . }}
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app: reminder-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "todo-app.reminder-service.fullname" . }}
  minReplicas: {{ .Values.eventServices.reminder.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.eventServices.reminder.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.eventServices.reminder.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.eventServices.reminder.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.eventServices.reminder.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.eventServices.reminder.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}