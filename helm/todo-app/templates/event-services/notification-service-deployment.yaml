apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todo-app.notification-service.fullname" . }}
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app: notification-service
spec:
  {{- if not .Values.eventServices.notification.autoscaling.enabled }}
  replicas: {{ .Values.eventServices.notification.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "todo-app.selectorLabels" . | nindent 6 }}
      app: notification-service
  template:
    metadata:
      annotations:
        {{- if .Values.dapr.enabled }}
        dapr.io/enabled: "true"
        dapr.io/app-id: "{{ .Values.dapr.notificationService.appId }}"
        dapr.io/app-port: "{{ .Values.dapr.notificationService.appPort }}"
        dapr.io/config: "{{ .Values.dapr.config }}"
        dapr.io/log-as-json: "true"
        {{- end }}
        {{- with .Values.eventServices.notification.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "todo-app.selectorLabels" . | nindent 8 }}
        app: notification-service
    spec:
      {{- with .Values.eventServices.notification.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "todo-app.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.eventServices.notification.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}-notification-service
          securityContext:
            {{- toYaml .Values.eventServices.notification.securityContext | nindent 12 }}
          image: "{{ .Values.eventServices.notification.image.repository }}:{{ .Values.eventServices.notification.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.eventServices.notification.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.eventServices.notification.service.port }}
              protocol: TCP
          {{- if .Values.dapr.enabled }}
          env:
            - name: DAPR_HTTP_ENDPOINT
              value: "http://localhost:3500"
            - name: DAPR_GRPC_ENDPOINT
              value: "http://localhost:50001"
            - name: KAFKA_BROKER
              value: {{ .Values.kafka.broker | quote }}
            - name: KAFKA_CONSUMER_GROUP
              value: "notification-consumer-group"
          {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.eventServices.notification.resources | nindent 12 }}
      {{- with .Values.eventServices.notification.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.eventServices.notification.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.eventServices.notification.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}