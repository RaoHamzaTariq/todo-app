apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app.notification-service.fullname" . }}
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app: notification-service
spec:
  type: {{ .Values.eventServices.notification.service.type }}
  ports:
    - port: {{ .Values.eventServices.notification.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-app.selectorLabels" . | nindent 4 }}
    app: notification-service