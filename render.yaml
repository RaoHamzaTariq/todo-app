services:
  - type: web
    name: backend-api
    plan: free
    env: docker
    dockerfilePath: backend/Dockerfile

  - type: private_service
    name: mcp-server
    plan: free
    env: docker
    dockerfilePath: backend/Dockerfile.mcp
