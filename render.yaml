services:
  - type: web
    name: backend-api
    plan: free
    env: docker
    dockerfilePath: backend/Dockerfile

  - type: web
    name: mcp-server
    plan: free
    env: docker
    dockerfilePath: backend/Dockerfile.mcp
